Local (Ollama) - Already working:
├── DeepSeek R1 1.5B (router/fast reasoning)
├── Qwen 3 4B (general tasks)
└── Qwen3-VL 2B (vision/documents)
└── Speech to Text Model

Cloud (APIs you HAVE):
├── Claude Sonnet 4.5 & Haiku 4.5 (complex reasoning)
├── GPT 4o (Fast classification (like Haiku's role)
- Simple queries that don't need Claude quality
- Burning through those $5 credits 😂)

├── Perplexity Sonar (research/current info)

Integration (Focus here):
└── Google Calendar API (OAuth + scheduling)

Architecture:
├── LangGraph (orchestration)
├── Alignment Agent (your CS+Ling angle)
├── Simple memory (last N + compression)
├── FastAPI backend
└── Electron UI (Cursor generates)